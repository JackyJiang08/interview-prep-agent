# Development dependencies. Installs the runtime set as well.
# Pinned so that a new lint release cannot turn CI red on an unchanged commit.
-r requirements.txt
pytest==8.4.1
ruff==0.16.1
# Optional tooling: tracing wrapper used only when LANGSMITH_TRACING=true,
# and the local graph viewer behind 'langgraph dev'. Neither is imported on
# the default offline paths.
langsmith==0.10.16
langgraph-cli==0.4.31
# Behavioral regression suites: trajectory matching for the offline matrix.
agentevals==0.0.9
